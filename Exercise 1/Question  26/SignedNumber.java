package Exercise;

public class SignedNumber {
public static void main(String[] args) {
	int i=-243;
	int j=243;
	//semi-colon at the end of if so the expression is evaluated but not apply condition
	if(i==j);
	System.out.println("numbers are same");
}
}
